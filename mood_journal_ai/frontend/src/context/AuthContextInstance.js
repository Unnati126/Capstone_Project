// only context
import { createContext } from "react";

/*export const AuthContext = createContext({
    token: null,
    login: () => {},
    logout: () => {},
});*/

export const AuthContext = createContext();